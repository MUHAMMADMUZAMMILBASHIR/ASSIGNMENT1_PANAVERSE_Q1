"use strict";
var array = new Array(3);
for (var i = 0; i < 5; i++) {
    console.log(array[i]);
}
console.log(array[5]);
// Logical error
// Three Types of intentional error
// Runtime / Execution
// Syntax / compileTime
// Logical Error
